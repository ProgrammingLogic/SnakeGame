{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Module",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/src/main.py",
            "console": "integratedTerminal",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build and Install"
        }
    ]
}